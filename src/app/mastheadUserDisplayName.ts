/**
 * Display name for the mock signed-in user. Used by the masthead user menu toggle
 * and homepage hero greeting. Replace with identity / auth when integrated.
 */
export const MASTHEAD_USER_DISPLAY_NAME = 'Ned Username';

/** Email for the mock signed-in user — used where the console would prefill the current user (e.g. support notifications). */
export const MASTHEAD_USER_EMAIL = 'nedusername@parasolinc.com';
