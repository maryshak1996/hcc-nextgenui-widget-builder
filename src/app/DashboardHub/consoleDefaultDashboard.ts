/** Re-exports for backward compatibility — see `prebuiltDashboards.ts` for definitions. */
export {
  CONSOLE_DEFAULT_BODY_TITLE,
  CONSOLE_DEFAULT_DASHBOARD_ID,
  createConsoleDefaultHubRow,
  getConsoleDefaultWidgets,
  isConsoleDefaultDashboardId,
  isConsoleDefaultHubRow,
  mergeConsoleDefaultIntoRows
} from '@app/DashboardHub/prebuiltDashboards';
